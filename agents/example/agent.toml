[agent]
id = "example"
# name = "Example"  # optional display name; falls back to id if unset
description = "Example agent — copy this directory to agents/<id>/ and customise"

[llm]
backend = "minimax"
model = "minimax-m2.7"
auth_profile = "minimax-default"
max_tokens = 4096
timeout_sec = 60

[tools]
allowed = []

[adapter]
type = "cli"
