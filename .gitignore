# Binaries
bin/
dist/
*.exe
/zlaw

# Go build artifacts
*.test
*.out
vendor/

# Local config (may contain credentials)
.fizzy.yaml
*.local.toml
.env
.env.*

# Runtime agent directories (use agents/example/ as template)
agents/*/
!agents/example/
!agents/example/**

# Local ZLAW_HOME for development (credentials, sessions, run sockets)
.zlaw/

# ZLAW_HOME runtime data (credentials and session history)
credentials.toml
sessions/

# Temporary session notes
WORKLOG.md

# Local Claude instructions (personal workflow, not shared)
CLAUDE.local.md

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
