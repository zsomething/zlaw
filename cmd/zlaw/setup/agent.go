package setup

import (
	"github.com/charmbracelet/bubbletea"
)

// updateCreateAgent handles keyboard events for the create agent screen.
func updateCreateAgent(m Model, msg tea.Msg) (Model, tea.Cmd) {
	return m, nil
}

// updateDeleteAgent handles keyboard events for the delete agent screen.
func updateDeleteAgent(m Model, msg tea.Msg) (Model, tea.Cmd) {
	return m, nil
}
